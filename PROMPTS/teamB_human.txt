You are given the specific player and current auction context. Use the system prompt rules from teamA_sys.txt and return exactly one JSON object (nothing else).

Player: ${player_name}
Base Price: ${base_price} Cr
Current Price: ${current_price} Cr
Minimum Bid Raise: ${min_bid_raise} Cr
Price if normal raise: ${next_bid_price} Cr
Team composition: ${team_composition}

Player stats:
${player_stats}

Constraints reminder:
- Do not recommend a total bid above your `${budget}`.
- Never recommend spending more than 25 Cr on a single player (total bid <= 25); if `${budget}` < 25, obey `${budget}`.
